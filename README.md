# IMPORTANT

IMPORTANT NOTE: when cloning, make sure to be in the ~ folder, otherwise it will not be installed propperlly and it might break your Windows subsystem for linux machine (that won't really happen, unless...)  
NOTE: this does not mean you have lost your wsl machine if you run it, close the wsl window then head to your home folder using File Explorer by going to Linux > <DistroName> > home > <Username> then erase the text in .bash_profile  
NOTE: also if you accadentlly cloned it somewhere else then ~ tell me then i can help you  
NOTE: if you don't have git try running: sudo apt update && sudo apt install git -y  
